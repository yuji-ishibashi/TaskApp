<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.techacademy.yuji.ishibashi.taskapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".TaskApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TaskApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InputActivity"
            android:label="タスク"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.TaskApp.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="jp.techacademy.yuji.ishibashi.taskapp.MainActivity" />
        </activity>
        <activity
            android:name=".EditCategoryActivity"
            android:label="カテゴリー"
            android:parentActivityName=".InputActivity"
            android:theme="@style/Theme.TaskApp.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="jp.techacademy.yuji.ishibashi.taskapp.InputActivity" />
        </activity>

        <receiver android:name=".TaskAlarmReceiver" />
    </application>

</manifest>